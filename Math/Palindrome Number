class Solution {
public:
    bool isPalindrome(int x) {
        string s=to_string(x);
        string res=s;
        reverse(s.begin(), s.end());
        if(s==res) return true;
        else return false;
        
    
    }
};
